{
  "name": "@shalkaar/security",
  "version": "1.0.0",
  "description": "SHALKAAR Security Package",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.2.10",
    "@shalkaar/logging": "workspace:*"
  }
}
